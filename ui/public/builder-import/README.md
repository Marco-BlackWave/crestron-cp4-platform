# Builder Import Staging

Copy your full Crestron UI Builder export into this folder.

Required file:
- `project-config.json`

Recommended:
- Keep all source folders and assets exactly as exported.
- Preserve relative paths used by page backgrounds/assets.

Runtime page URL:
- `http://localhost:5173/#/builder-runtime`
